# 所需部件

### Accordion（手风琴）

似乎不需要

### Alert（警告）

看看根目录下的AGENTS.md和PROJEC.md,快速上手了解这个项目的整体架构和技术栈：

````

Aspect Ratio


```
````

npx shadcn@latest add alert

npx shadcn@latest add alert-dialog

npx shadcn@latest add badge

npx shadcn@latest add breadcrumb

npx shadcn@latest add button

npx shadcn@latest add button-group

npx shadcn@latest add card

npx shadcn@latest add carousel

npx shadcn@latest add chart

npx shadcn@latest add collapsible

npx shadcn@latest add checkbox

npx shadcn@latest add combobox

npx shadcn@latest add command

npx shadcn@latest add context-menu

npx shadcn@latest add table

npm install @tanstack/react-table

npx shadcn@latest add dialog

npx shadcn@latest add drawer

npx shadcn@latest add dropdown-menu

npx shadcn@latest add empty

npx shadcn@latest add hover-card

npx shadcn@latest add input

npx shadcn@latest add input-group

npx shadcn@latest add label

npx shadcn@latest add menubar

npx shadcn@latest add navigation-menu

npx shadcn@latest add pagination

npx shadcn@latest add popover

npx shadcn@latest add resizable

npx shadcn@latest add scroll-area

npx shadcn@latest add select

npx shadcn@latest add separator

npx shadcn@latest add sidebar

npx shadcn@latest add skeleton

npx shadcn@latest add slider

npx shadcn@latest add sonner

npx shadcn@latest add spinner

npx shadcn@latest add switch

npx shadcn@latest add table

npx shadcn@latest add tabs

npx shadcn@latest add textarea

npx shadcn@latest add toggle

npx shadcn@latest add toast

npx shadcn@latest add sheet

---

npx shadcn@latest add @aceternity/stateful-button-demo

npx shadcn@latest add @aceternity/resizable-navbar-demo

---

## empty需要丰富

---

## 给每个card的角落（暂定左上角）加一个正方形的小按钮src\components\ui\toggle.tsx，里面的内容是五角星（依旧使用lucide），里面无文本，纯图标，悬浮时产生src\components\ui\tooltip.tsx英文提示。这个按键作用是收藏/取消收藏的标记。收藏/取消收藏之后有toast提示。（import { toast } from "sonner";）

src\components\library\WallpaperSidebar.tsx里面，在壁纸编号，壁纸大小的两个badge下面、tag，type上面，增加（插入）两个按键：

1.设定/修改壁纸nickname，，点击打开一个src\components\ui\dialog.tsx，让用户输入nickname。设定之后弹出toast提示。

2.收藏/取消收藏的五角星，是一个src\components\ui\toggle.tsx，这里的状态与src\components\library\WallpaperCard.tsx中的那个收藏五角星保持一致。收藏/取消收藏之后有toast提示

鉴于sidebar可能足够宽，建议这两个按键可以做成长形的，"图标+文字"的显示格式

compact需要更多功能按键引入

---

## ⭐路线 A（留在 AppNavbar，定位为全局搜索），这也是一个非常有前瞻性的决定。现代应用（如 VS Code、Notion、macOS）都偏向于在顶部提供一个统一的“超级搜索框”。

既然目前的搜索框和 searchQuery 状态已经在全局生效了，要走好路线 A，最核心的任务是让这个搜索框具备**“上下文感知（Context-Aware）”**能力。

也就是说：

动态提示：在壁纸库显示 Search library...，在设置页显示 Search settings...。

切换隔离：当用户在不同页面之间切换时，自动清空上一页的搜索词。

按需显隐数字：目前只有壁纸库实现了实时过滤，所以 XX results 应该只在壁纸库显示。

先把现在的搜索框改造成shadcn的src\components\ui\command.tsx（我更倾向使用其中的group，scrollable），

这个弹出的command下伸出来的菜单，就先按照我们的library/monitor/settings分组（或者可以再加上common/else常规功能），

library下有显示最近播放与收藏的壁纸，

monitor下不需要具体内容(因为这个页面太小了)，

settings下举例出一些常规设置（比如音量大小，fps等等）点击后跳转到settings页面下的具体设置的位置；

common就是基本功能：播放，停止，刷新，退出应用等。

每个功能项都可以点击进入相应的页面（比如显示最近播放与收藏的壁纸就进入library页面，并且默认选中最近播放或者收藏的tab）。

当匹配不到具体选项时，默认是去library里面找壁纸（因为壁纸的搜索方式有id，名称等）

这个command菜单的设计可以参考VSCode的command palette，或者其他一些常见的command菜单设计。

---

## ❓版本号 0.1.0 来自两个地方（需要保持同步）：

1. Rust 后端（src-tauri/Cargo.toml）
   [package]
   version = "0.1.0"
   → lib.rs 中通过 env!("CARGO_PKG_VERSION") 读取
2. Tauri 配置（src-tauri/tauri.conf.json）
   {
   "version": "0.1.0"
   }
   → 用于应用打包时的版本标识

两者必须一致。如果要更新版本号，需要同时修改这两个文件。

目前 Tauri 版本的默认版本号如下：

当前版本配置

| 文件                      | 版本号 | 说明            |
| ------------------------- | ------ | --------------- |
| package.json              | 0.1.0  | NPM 包版本      |
| src-tauri/Cargo.toml      | 0.1.0  | Rust crate 版本 |
| src-tauri/tauri.conf.json | 0.1.0  | Tauri 应用版本  |

| Tauri 框架版本
| @tauri-apps/api: ^2.10.1 (最新 v2 版本)
| @tauri-apps/cli: ^2 (浮动版本)
| tauri (Rust): 2 (浮动版本)

总结: 你的应用目前使用的是 0.1.0 作为默认版本号，这是一个典型的早期开发版本标识。Tauri 框架本身使用的是 v2 系列（最新稳定版）。

---

## assert dictionary 加一个info（lucide），

```
import { Info } from 'lucide-react';

const App = () => {
  return (
    <Info />
  );
};

export default App;

```

使用类似lwg-gui-tauri/src/components/ui/hover-card.tsx的，(或者tooltip)

告诉用户自动监测的路径如下：

```
~/.steam/steam/steamapps/common
~/.local/share/Steam/steamapps/common
~/.var/app/com.valvesoftware.Steam/.local/share/Steam/steamapps/common
~/snap/steam/common/.local/share/Steam/steamapps/common
```

---

## go screenshot按键，点击跳转回library,同时高亮navbar的screenshot按键，弹出一个hovercard提醒用户“clicl me to screenshot!!”

---

## settings下的各个组件似乎有最大宽度限制，当我把整个gui的宽度调大时，组件并没有随之变大，保持在一个较小的宽度范围内。需要检查一下这些组件的样式设置，看看是否有max-width或者类似的属性限制了它们的宽度。

---

## 壁纸名显示：支持markdown语法

---

## （sidebar）统一使用lwg-gui-tauri/src/components/ui/separator.tsx代替自定义的分割线组件

---

## ❓history（screenshot/play）两项列表，可以考虑使用Table/DataTable组件来实现，或者至少参考它们的设计风格来优化列表的展示效果。

---

## 有硬编码的颜色问题

library里面sidebar的壁纸名称，nickname，favorite这几个

monitor里面的各个标题（System Monitor，Process Details，Screenshot History），以及Screenshot History下面的clear按键，各个壁纸的记录显示的名称什么的

---

## ❓可以考虑给navbar也引入控制颜色主题的switch按键（太阳，月亮，monitor），或者说直接把设置里的哪个开关搬过去

---

## ⭐📋 Legacy: py_GUI（原版 Python 版）使用的 JSON 文件清单 (仅供参考)

1️⃣ 用户配置目录下的 JSON 文件

配置目录位置：~/.config/linux-wallpaperengine-gui/

| JSON 文件    | 位置                                             | 用途                         |
| ------------ | ------------------------------------------------ | ---------------------------- |
| config.json  | ~/.config/linux-wallpaperengine-gui/config.json  | 主配置文件，存储所有用户设置 |
| history.json | ~/.config/linux-wallpaperengine-gui/history.json | 播放历史记录（最多30条）     |

---

2️⃣ Steam Workshop 壁纸目录下的 JSON 文件

位置：~/.local/share/Steam/steamapps/workshop/content/431960/{wallpaper_id}/

| JSON 文件    | 用途                                   | 读取位置                |
| ------------ | -------------------------------------- | ----------------------- |
| project.json | 壁纸元数据（标题、描述、类型、标签等） | WallpaperManager.scan() |

每个壁纸文件夹下都有一个 project.json，包含：

- title - 壁纸标题
- description - 壁纸描述
- type - 类型（Scene/Web/Video）
- tags - 标签列表
- preview - 预览图文件名
- file - 主文件
- contentrating - 内容分级
- version - 版本

---

3️⃣ config.json 结构（DEFAULT_CONFIG）

```json
{
  "fps": 30,
  "volume": 0,
  "scaling": "default",
  "silence": true,
  "noFullscreenPause": false,
  "disableMouse": false,
  "noautomute": false,
  "noAudioProcessing": false,
  "disableParallax": false,
  "disableParticles": false,
  "clamping": "clamp",
  "lastWallpaper": null,
  "lastScreen": null,
  "wallpaperProperties": {},
  "wallpaperNicknames": {}, // 昵称数据（由 NicknameManager 管理）
  "screenshotDelay": 20,
  "screenshotRes": "3840x2160",
  "preferXvfb": true,
  "active_monitors": {},
  "cycleEnabled": false,
  "cycleInterval": 15,
  "cycleOrder": "random",
  "assetsPath": null,
  "wayland_only_active": false,
  "wayland_ignore_appids": "",
  "compact_mode": false
}
```

---

4️⃣ history.json 结构

```json
[
  {
    "id": "12345678",
    "title": "壁纸标题",
    "preview": "/path/to/preview.jpg",
    "timestamp": "2026-03-08T17:30:00"
  }
  // ... 最多 30 条
]
```

---

5️⃣ 远程 JSON（GitHub API）

| 来源                                                                                | 用途         | 管理类        |
| ----------------------------------------------------------------------------------- | ------------ | ------------- |
| https://api.github.com/repos/Suhoiyis/gui-for-linux-wallpaperengine/releases/latest | 检查应用更新 | UpdateChecker |

---

📊 文件操作汇总

| 操作 | 文件         | 类                      |
| ---- | ------------ | ----------------------- |
| 创建 | config.json  | ConfigManager           |
| 创建 | history.json | HistoryManager          |
| 读取 | config.json  | ConfigManager.load()    |
| 读取 | history.json | HistoryManager.\_load() |
| 读取 | project.json | WallpaperManager.scan() |
| 写入 | config.json  | ConfigManager.save()    |
| 写入 | history.json | HistoryManager.\_save() |

---

这就是 Python 版本中所有涉及 JSON 文件的使用情况。**注意：新版的 Tauri 应用已采用全新的 JSON 文件分离方案，不再与 Python 版的 config.json 完全向后兼容。**

区分“缓存”和“用户数据”

```
~/.config/lwg/
  └── config.json        # 核心配置（兼容 Python 版）

~/.cache/lwg/            # 可被系统清理
  ├── thumbnails/        # 缩略图缓存
  ├── library_index.json # 库扫描索引
  └── temp/              # 临时解压文件

~/.local/share/lwg/      # 如果有不可丢失的用户数据（可选）
  ├── favorites.json     # 收藏列表（如果不想放 config）
  └── ratings.db         # SQLite 数据库（用 sql 插件管理）
```

#### 📂 Tauri 版 JSON 文件分离方案 (当前实现)

##### 1. 用户配置 (User Preferences)
位置: `~/.config/linux-wallpaperengine-gui/config.json`

存储用户的手动设置，不包含运行时状态。

```json
{
  "fps": 30,
  "volume": 50,
  "scaling": "default",
  "clamping": "clamp",
  "muteAudio": false,
  "noFullscreenPause": false,
  "disableMouse": false,
  "noAutomute": false,
  "noAudioProcessing": false,
  "disableParallax": false,
  "disableParticles": false,
  "screenshotDelay": 20,
  "screenshotRes": "3840x2160",
  "preferXvfb": true,
  "cycleEnabled": false,
  "cycleInterval": 15,
  "cycleOrder": "random",
  "workshopPath": "/path/to/workshop",
  "assetsPath": null,
  "waylandOnlyActive": false,
  "waylandIgnoreAppids": "",
  "compactMode": false,
  "wallpaperProperties": {},
  "wallpaperNicknames": {}
}
```

##### 2. 运行时状态 (Runtime State)
位置: `~/.local/state/linux-wallpaperengine-gui/state.json`

直接映射：屏幕名 → 壁纸状态。可以安全删除（删除后应用将以空状态启动）。

```json
{
  "HDMI-1": {
    "wallpaperId": "2874425843",
    "isPlaying": true
  },
  "eDP-1": {
    "wallpaperId": "2810924556",
    "isPlaying": false
  }
}
```

**字段说明**:
- `wallpaperId`: 壁纸 ID
- `isPlaying`: 是否正在播放
  - `true`: 该屏幕正在播放此壁纸
  - `false`: 该屏幕已停止播放，但保留历史记录

**行为**:
- 播放壁纸 → 设置 `isPlaying: true`
- 停止壁纸 → 设置 `isPlaying: false`（保留条目，不删除）
- 应用重启后可恢复 `isPlaying: true` 的屏幕

##### 3. 播放历史 (Playback History)
位置: `~/.cache/linux-wallpaperengine-gui/history.json`

遵循 XDG 缓存规范，存储最近播放的壁纸记录（最多 30 条）。

```json
[
  {
    "id": "2874425843",
    "title": "壁纸标题",
    "preview": "/path/to/preview.jpg",
    "timestamp": "2026-03-08T17:30:00"
  }
]
```

---

#### 🎯 关键设计要点

1. **破坏性变更**: 不再兼容 Python 版本的 `config.json`。
2. **状态持久化**: `state.json` 存储所有屏幕的壁纸状态，包括已停止的壁纸（`isPlaying: false`）。
3. **可丢弃性**: `state.json` 和 `history.json` 都可以随时删除，系统会自动重新生成。
4. **符合 XDG 规范**: 严格区分配置 (Config)、状态 (State) 和缓存 (Cache) 目录。

#### 🔄 操作与 state.json 更新

| 操作 | state.json 更新 |
|------|------------------|
| 播放壁纸 | 设置该屏幕 `isPlaying: true` |
| 停止单屏幕 | 设置该屏幕 `isPlaying: false` |
| 停止所有 | 设置所有屏幕 `isPlaying: false` |
| 随机壁纸 | 调用播放壁纸逻辑 |
| 应用重启 | 读取 state.json，恢复 `isPlaying: true` 的屏幕 |



## ⭐LOG

你的日志系统已经正常工作了！从你的终端输出可以看到：
已捕获的日志 (在 GUI 日志查看器中可见):
[18:25:15] [info] [Engine] Running with: /opt/linux-wallpaperengine...
[18:25:15] [error] [Engine] Failed to initialize GLEW: No GLX display
...
未捕获的应用程序日志 (仅在终端可见):
🐧 [Linux] 初始化应用状态...
✅ [Linux] 扫描到 39 张壁纸
▶️ [Rust] 正在应用壁纸: ...
✅ [Rust] 壁纸应用成功！

---

🎯 后续改进
将应用程序日志 (println!) 添加到日志系统需要更仔细的重构，因为需要：

1. 在每个 Tauri 命令中访问 log_manager
2. 正确处理生命周期和错误
   这是一个较大的重构任务。你想现在继续实现，还是先使用当前的日志系统？

日志来源定义

来源 定义 应该显示的内容
Engine linux-wallpaperengine 进程的 stdout/stderr 引擎运行状态、视频解码信息、硬件加速、错误信息
GUI Tauri 应用的前端/后端操作 用户操作、UI 事件、前端状态变化
Core Rust 核心库 (lwg-core) 配置解析、内部逻辑、工具函数
Controller WallpaperController 壁纸应用/停止、进程管理、屏幕操作

---

🔍 当前实现状态

来源 状态 内容
Engine ✅ 已实现 进程 stdout/stderr 输出
GUI ⚠️ 未使用 需要添加 log_manager.info(LogSource::GUI, ...) 调用
Core ⚠️ 未使用 需要 lwg-core 内部添加日志调用
Controller ⚠️ 未使用 需要 WallpaperController 中添加日志调用

---

📋 各来源应该显示的具体内容

Engine (已实现 ✅)

```
[18:25:15] [info] [Engine] Running with: /opt/linux-wallpaperengine...
[18:25:15] [error] [Engine] Failed to initialize GLEW: No GLX display
[18:25:22] [info] [Engine] Using hardware decoding (vaapi-copy)
```

GUI (待实现)

```
[18:25:15] [info] [GUI] Scanning wallpaper library...
[18:25:15] [info] [GUI] Found 39 wallpapers
[18:25:15] [info] [GUI] Applying wallpaper: 3310692710
[18:25:15] [info] [GUI] Wallpaper applied successfully
```

Controller (待实现)

```
[18:25:15] [info] [Controller] Starting wallpaper process
[18:25:15] [info] [Controller] Engine started with PID 12345
[18:25:15] [info] [Controller] Stopping wallpaper...
```

Core (待实现)

```
[18:25:15] [info] [Core] Config loaded from /home/user/.config/...
[18:25:15] [info] [Core] Parsing wallpaper project.json
```

---

🎯 建议的分工

来源 触发时机
GUI 用户交互、Tauri 命令执行
Controller 进程管理、壁纸应用/停止
Core 配置读写、内部工具函数
Engine 引擎进程输出（已实现）

---

## ⭐参考tauri官方文档尝试使用tauri的插件替代现在的东西

---

### tray功能的实现方案对比

调研结果

对比分析

| 方案                         | 优点                                   | 缺点                            |
| ---------------------------- | -------------------------------------- | ------------------------------- |
| A. 使用 Tauri 内置 Tray      | 原生集成、同一进程、无需 IPC、API 简单 | 功能相对简单，缺少高级特性      |
| B. 引用 tray_rs 作为 sidecar | 复用现有代码、功能完整                 | 需要维护 IPC 通信、进程管理复杂 |
| C. 将 tray_rs 逻辑重写到     | Tauri 原生集成 + 功能完整              | 开发工作量较大                  |

Tauri v2 内置 Tray 功能

// Cargo.toml
tauri = { version = "2.0.0", features = [ "tray-icon" ] }
// lib.rs
use tauri::tray::TrayIconBuilder;
TrayIconBuilder::new()
.icon(app.default_window_icon().unwrap().clone())
.menu(&menu)
.on_tray_icon_event(|tray, event| {
// 左键点击显示/隐藏窗口
})
.on_menu_event(|app, event| {
// 菜单点击处理
})
.build(app)?;

Tauri v2 支持的功能：

- ✅ 托盘图标
- ✅ 右键菜单
- ✅ 左键/右键事件
- ✅ 动态切换图标
- ✅ Tooltip
  tray_rs 高级功能：
- ✅ 状态感知动态图标（运行/停止）
- ✅ 多显示器状态显示（Pango 格式化）
- ✅ Abstract Namespace Sockets（零残留）
- ✅ 进程生命周期绑定（父进程死亡自动退出）

---

我的建议
推荐方案 A：使用 Tauri 内置 Tray
理由：

1. Tauri v2 的 tray 功能已经足够满足基本需求
2. 同一进程，无需复杂的 IPC 通信
3. 状态感知图标可以在 Tauri 中实现（通过 tray.set_icon()）
4. 多显示器状态可以通过动态 tooltip 实现
5. 代码更简洁，维护成本更低
   tray_rs 的 Abstract Sockets 等高级特性对于 Tauri 来说不是必需的，因为 Tauri 的 tray 是内置在同一进程中的，不存在进程间通信的问题。

完整对比分析

技术架构差异

| 对比项     | Tauri 内置            | Tray           | tray_rs           |
| ---------- | --------------------- | -------------- | ----------------- |
| 底层库     | libappindicator       | (GTK-based)    | ksni (DBus-based) |
| 进程架构   | 同进程                | 独立进程 + IPC |
| Linux 依赖 | GTK3, libappindicator | 仅 DBus        |

| 功能对比 |

| 功能                 | Tauri 内置        | tray_rs         | 差距分析     |
| -------------------- | ----------------- | --------------- | ------------ |
| 托盘图标             | ✅ 支持           | ✅ 支持         | 相同         |
| 右键菜单             | ✅ 支持           | ✅ 支持         | 相同         |
| 左键点击事件         | ✅ 支持           | ✅ 支持         | 相同         |
| 动态切换图标         | ✅ 支持           | ✅ 支持         | 相同         |
| Tooltip (悬停提示)   | ❌ Linux 不支持   | ✅ 支持         | 关键差距     |
| Pango Markup 格式化  | ❌ 不支持         | ✅ 支持         | 关键差距     |
| 多显示器状态显示     | ⚠️ 需通过菜单实现 | ✅ Tooltip 显示 | 实现方式不同 |
| 进程生命周期绑定     | ✅ 自动 (同进程)  | ✅ 手动实现     | Tauri 更简单 |
| Abstract Sockets IPC | 不需要            | ✅ 需要         | Tauri 更简单 |

---

### 结论：**无法完全复刻**

**关键差距**：

1. **Linux Tooltip 不支持** - Tauri 文档明确说明 `tooltip` 在 Linux 上不支持
2. **Pango Markup** - tray_rs 使用 Pango markup 在 tooltip 中显示格式化的多显示器状态，Tauri 无法实现

---

替代方案
如果选择 Tauri 内置 Tray，需要用其他方式实现状态显示：

tray_rs 方式 Tauri 替代方案

Tooltip 显示状态 菜单项动态更新显示状态
Pango 格式化多屏状态 菜单子项分别显示
左键点击 toggle 窗口 ✅ 相同实现
右键菜单 ✅ 相同实现
动态图标 (运行/停止) ✅ 相同实现

---

我的建议
推荐：方案 A (Tauri 内置 Tray) + 适度功能调整
理由：

1. 核心功能都能实现：图标、菜单、事件、动态图标
2. Tooltip 差距可用菜单弥补：在菜单顶部添加状态项
3. 架构更简单：同进程，无需 IPC，无需进程管理
4. 维护成本更低：少一个独立进程
   但如果你坚持需要 Tooltip 显示多显示器状态，则需要选择方案 B（引用 tray_rs 作为 sidecar）。

---

## ❓再次限制sidebar的最大宽度，有的缩略图确实分辨率小，放太大了会很难看

---

## compact下lwg-gui-tauri/src/components/compact/CompactCarousel.tsx不能显示出我现在的壁纸预览图了，显示不出来，检查一下这个组件的实现，看看是不是哪里有问题导致图片无法加载或者显示。

同时对比一下lwg-gui-tauri/src/components/WallpaperCard.tsx这里面的显示是没有问题的

---

npm/cargo使用的组件有没有多余的，检查一下

这些组件的版本控制上是怎样的，必须保持最新还是要使用特定版本

---

libraryheader里面的页码跳转似乎因为误操作而无法实际生效

---

## 按键尽量统一颜色（brand？）可以考虑一下

---

## Tauri API

### [应用程序大小](https://tauri.app/zh-cn/concept/size/)

虽然 Tauri 默认提供非常小的二进制文件，但稍微推一下极限也无妨，因此这里有一些技巧和建议，以达到最佳效果。

Cargo 配置
您可以对项目进行的最简单的前端无关的大小优化之一是为其添加一个 Cargo 配置文件。

根据您使用的是 stable 还是 nightly 的 Rust 工具链，您可以使用的选项会有所不同。除非您是高级用户，建议您坚持使用稳定版工具链。

Stable

```src-tauri/Cargo.toml
[profile.dev]
incremental = true # 以较小的步骤编译您的二进制文件。

[profile.release]
codegen-units = 1 # 允许 LLVM 执行更好的优化。
lto = true # 启用链接时优化。
opt-level = "s" # 优先考虑小的二进制文件大小。如果您更喜欢速度，请使用 `3`。
panic = "abort" # 通过禁用 panic 处理程序来提高性能。
strip = true # 确保移除调试符号。
```

### [App Icons](https://tauri.app/zh-cn/develop/icons/)

Tauri ships with a default iconset based on its logo. This is NOT what you want when you ship your application. To remedy this common situation, Tauri provides the icon command that will take an input file ("./app-icon.png" by default) and create all the icons needed for the various platforms.

### [分发](https://tauri.app/zh-cn/distribute/)

- Appimage
- AUR
- FlatHub

### [启动画面](https://tauri.app/zh-cn/learn/splashscreen/)

在 Tauri 应用中实现一个基本的启动画面功能。 实现方法非常简单，启动画面实际上就是在启动应用前执行一些繁重的设置任务时 创建一个新窗口来显示一些内容，然后在设置完成后关闭它。

### [Tray](https://tauri.app/zh-cn/learn/system-tray/)

### [自动启动](https://tauri.app/zh-cn/plugin/autostart/)

在系统启动时自动启动应用程序。

### [剪切板](https://tauri.app/zh-cn/plugin/clipboard/)

使用剪贴板插件读取和写入系统剪贴板。

### [对话框](https://tauri.app/zh-cn/plugin/dialog/)

本机系统对话框，用于打开和保存文件，以及消息对话框。

### [文件系统](https://tauri.app/zh-cn/plugin/file-system/)

访问文件系统。

### [全局快捷方式](https://tauri.app/zh-cn/plugin/global-shortcut/)

注册全局快捷方式。

### [日志](https://tauri.app/zh-cn/plugin/logging/)

为你的 Tauri 应用程序配置日志记录。

### [通知](https://tauri.app/zh-cn/plugin/notification/)

使用通知提示插件以向你的用户发送原生通知。

自动轮换时可用

### [指定打开应用](https://tauri.app/zh-cn/plugin/opener/)

此插件允许你使用特定或者默认的应用程序打开文件或者 URL。它还可以在系统文件管理器中“显示”这些文件。

可用于跳转steam workshop URL

### [操作系统信息](https://tauri.app/zh-cn/plugin/os-info/)

使用操作系统信息插件读取操作系统信息。

another:https://github.com/HuakunShen/tauri-plugin-system-info

### [持久化作用域（Persisted Scope）](https://tauri.app/zh-cn/plugin/persisted-scope/)

保存文件系统和资源作用域，并在应用重新打开时恢复它们。

### [定位器（Positioner）](https://tauri.app/zh-cn/plugin/positioner/)

把窗口放在你熟悉的地方。

这个插件是 electron-positioner 的 Tauri 版本。

### [进程](https://tauri.app/zh-cn/plugin/process/)

这个插件提供了访问当前进程的 API。要生成子进程，请参阅 shell 插件。

### [Shell](https://tauri.app/zh-cn/plugin/shell/)

访问系统 shell。允许你生成子进程。

### [单例](https://tauri.app/zh-cn/plugin/single-instance/)

使用单实例插件确保 Tauri 应用程序在同一时间只运行单个实例。

### [更新](https://tauri.app/zh-cn/plugin/updater/)

自动使用更新服务器或静态 JSON 更新你的 Tauri 应用程序。

### [窗口状态](https://tauri.app/zh-cn/plugin/window-state/)

保存窗口位置和大小，并在应用程序重新打开时恢复它们。

### 三、可选 / 看情况加的插件

1. clipboard-manager
   场景：你需要前端直接读写剪贴板内容，比如：
   “复制当前壁纸信息”；
   “粘贴壁纸路径”。
   建议：
   如果你暂时不做这类功能，可以不加。
2. upload
   官方 upload 插件用于 HTTP 文件上传
   github.com
   。

场景：如果你打算：
上传用户自制壁纸到某个服务器；
或者上传日志/崩溃报告。
建议：
有这类需求时再加，目前可以暂缓。3. persisted-scope
场景：你使用 fs 插件让前端动态访问用户选择的目录，希望这些作用域在重启后仍然有效
github.com
。
建议：
如果你把文件访问都集中在 Rust 侧，作用域固定（比如只读 workshopPath），可以不加。

### 四、不太推荐现在就上的插件

1. global-shortcut（在 Wayland 下有坑）
   如前所述，global-shortcut 在 Wayland GNOME 下有回调不触发的问题
   github.com
   。
   你的主要平台是 Linux，Wayland 用户会越来越多。
   建议：
   如果你确实需要全局快捷键，可以考虑：
   只在 X11 下启用；
   或者用“实验性功能”方式，并在文档里说明 Wayland 暂不支持。
2. positioner（被 window-state 覆盖）
   positioner 主要是“把窗口放在常见位置”
   github.com
   。
   你已经有 window-state，它会记住用户最后的位置和大小
   tauri.app
   。
   建议：
   除非你有“首次启动 → 强制居中”之类的需求，否则可以不加。
3. stronghold
   stronghold 是一个加密数据库
   github.com
   。
   你已经有 config.json，并且主要目标是兼容 Python 版本，不太适合再引入一个复杂的安全存储。
   建议：
   一般配置和缓存用 store / sql + 文件系统加密（如 Linux 的磁盘加密）即可，stronghold 有点重。
